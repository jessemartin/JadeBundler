var vows = require('vows'),
    assert = require('assert'),
    suite = vows.describe('Jade bundler');

suite.addBatch({
  'The number 3': {
    topic: function () {
      return 3;
    },
    'should be 3': function (num) {
      assert(num === 3);
    }
  }
});

suite['export'](module);